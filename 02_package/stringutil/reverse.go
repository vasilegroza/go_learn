// package stringutil contains utility functions for working with strings
package stringutil

// package scope
// the functions witch have lowercase name start won't be visible outside the package

func Reverse(s string) string {
	return reverse_two(s)
}
